buildscript {
    repositories { google(); mavenCentral(); maven { url = uri("https://jitpack.io") } }
}
allprojects {
    repositories { google(); mavenCentral(); maven { url = uri("https://jitpack.io") } }
}
